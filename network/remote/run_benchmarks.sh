#!/usr/bin/env bash
set -euo pipefail

echo "This framework runs network benchmark pairs from the local orchestrator."
echo "Use network/scripts/run_benchmarks.sh so server and client commands can be coordinated over SSH."
